// Generated by LiveScript 1.5.0
var R, relativeTime;
R = require('ramda');
relativeTime = require('../modules/relative-time');
module.exports = function(log, tasks){
  var list, this$ = this;
  list = R.groupBy(function(it){
    return it.status;
  })(
  tasks);
  return [['done', "Done tasks."], ['new', "ToDo."], ['doing', "Doing tasks."]].forEach(function(arg$){
    var type, title, ref$;
    type = arg$[0], title = arg$[1];
    if (list[type]) {
      log.info(title);
      return (ref$ = list[type]) != null ? ref$.forEach(function(arg$){
        var id, name, modified;
        id = arg$.id, name = arg$.name, modified = arg$.modified;
        return log.info("  " + id + ": " + name + " (" + relativeTime(modified) + ")");
      }) : void 8;
    }
  });
};